# Core (always needed)
langchain-core>=0.3
pydantic>=2.6
python-dotenv>=1.0

# Option A — Hugging Face Inference API (needs HUGGINGFACEHUB_API_TOKEN)
langchain-huggingface>=0.1
huggingface-hub>=0.25

# Option B — local model via Ollama (no token; install Ollama separately from ollama.com)
langchain-ollama>=0.2

# Connector — vendored tinder.py (src/vendor/tinder) talks to api.gotinder.com over HTTP
requests>=2.31
